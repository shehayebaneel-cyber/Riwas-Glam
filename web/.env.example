# URL of the hosted API. Leave EMPTY for local dev (Vite proxies /api -> :4200).
# In production (Vercel) set this to your Render URL, e.g.
# VITE_API_URL=https://riwasglam-api.onrender.com
VITE_API_URL=
